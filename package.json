{
  "name": "claude-speak",
  "version": "2.0.0",
  "description": "Voice output layer for Claude Code — passive spoken summaries and active voice capability",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check:dist": "node build.mjs && git diff --quiet HEAD -- dist/ || (echo 'dist/ is stale — commit the rebuild' && exit 1)",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.4",
    "openai": "^4.80.0",
    "punycode": "^2.3.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
